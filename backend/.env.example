# Backend Environment Variables for LOCAL DEVELOPMENT
# Copy this file to .env and update with your actual values

# MongoDB connection string
# Production: Set this in Render Dashboard → Environment Variables
DATABASE_URL=mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/dashpay

# JWT Secret - Use a strong random string (minimum 32 characters)
# Generate one using: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# Production: Set this in Render Dashboard → Environment Variables
JWT_SECRET=your_strong_jwt_secret_here_minimum_32_characters

# Server Port
# Local: 3000
# Production: 10000 (automatically set by Render)
PORT=3000

# Frontend URL for CORS
# Local: http://localhost:5173
# Production: https://dashpay-nu.vercel.app (set in Render Dashboard)
FRONTEND_URL=http://localhost:5173
