# Backend Environment Variables

# MongoDB connection string
DATABASE_URL=mongodb+srv://username:password@cluster.mongodb.net/dashpay?retryWrites=true&w=majority

# JWT Secret - Use a strong random string (minimum 32 characters)
# Generate one using: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your_strong_jwt_secret_here_minimum_32_characters

# Server Port (automatically set by hosting platforms like Render)
PORT=3000

# Frontend URL for CORS (set this to your deployed frontend URL)
FRONTEND_URL=http://localhost:5173
